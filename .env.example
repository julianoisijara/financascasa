# Credenciais OAuth do Google Cloud (tipo "Aplicativo para computador").
# Copie para .env antes de rodar `npm run dev` ou `npm run build:*`.
# Também podem ser informadas dentro do app, em Configurações > Google Drive.
MAIN_VITE_GOOGLE_CLIENT_ID=
MAIN_VITE_GOOGLE_CLIENT_SECRET=
